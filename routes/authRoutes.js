const { router } = require("../controllers/authController");

// normal flow should go like this
// router.post('/login', authController.login);
// router.post('/register', authController.register);

module.exports = router;
